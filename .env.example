# Root environment file.
#
# This file is only used by the database tooling (drizzle-kit) inside
# packages/db. Local secret copies in apps/*/.env are gitignored.
# Copy each example next to it and fill in real values before running anything.

# Neon Postgres connection string. Create a project at https://console.neon.tech
# and copy the pooled or direct connection string. Must also be copied into
# apps/api/.env so the API server can use it at runtime.
#

DATABASE_URL=postgresql://neondb_owner:npg_CY3iVKl8DZPE@ep-misty-credit-aef4q69u-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
